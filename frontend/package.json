{
  "name": "lisa-me",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1 --port 8789",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1 --port 8789",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "astro": "7.3.3"
  }
}
